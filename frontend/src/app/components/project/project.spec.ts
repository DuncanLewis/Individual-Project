/*import {Project} from './project';

describe('Project', () => {
  it('should create an instance', () => {
    expect(new Project()).toBeTruthy();
  });

  it('should accept values given by constructor', () => {
      let project = new Project({
          name: 'F0192A', //Todo: update this unit test with all attributes
          status: 'upcoming'
      });
      expect(project.name).toEqual('F0192A');
      expect(project.status).toEqual('upcoming');
  })
});
*/